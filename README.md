# ryanlazar.dev

Personal portfolio — HTML/CSS/vanilla JS, no framework, no build step.
Deployed on Cloudflare Pages; every push to `main` goes live automatically.

Run locally: any static server, e.g. `python3 -m http.server 8080`.
